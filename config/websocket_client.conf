; Asterisk uses this for External Media WebSocket (audio to DGX).
; spark-9965.local → 192.168.1.72 (DGX Spark)
; Or set SPARK_IP/DGX_IP in agent/.env so the agent passes host:port directly.

[media_connection1]
type = websocket_client
uri = ws://192.168.1.72:8787/media
protocols = media
connection_type = per_call_config
connection_timeout = 5000
reconnect_interval = 1000
reconnect_attempts = 3
tls_enabled = no
